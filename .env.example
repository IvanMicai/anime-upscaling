# Processing directory (must match host path for child container mounts)
PROCESS_DIR=/mnt/SSD2/process

# Ports
API_PORT=4751
APP_PORT=4750

# App only
AUTH_PASSWORD=change-me
AUTH_SECRET=change-me
API_URL=http://api:4751
